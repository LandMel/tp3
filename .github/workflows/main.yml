on: push
name: post package
jobs:
  package-post:
    name: post
    runs-on: ubuntu-latest
    steps:

    - name: Get latest codes
      uses: actions/checkout@v2
      
    - name: NPM Publish
      run: npm publish
